<issue>
  <div class="issue-row">
    <h4>{ opts.issue.title }</h4>
    <img src={opts.issue.avatar_url}/>
      <p>{ opts.issue.repo_name}</p>
      <p>{ opts.issue.first_line }</p>
  </div>
</issue>
